# Root reference only. Service-specific env files will live inside each app folder.

# Frontend (Next.js)
NEXT_PUBLIC_BASE_URL=http://localhost:3000
MUSICSYNC_APP_MODE=mock
SYNC_WRITE_ENABLED=false

# Real-data read-only validation (frontend)
SUPABASE_URL=
SUPABASE_SERVICE_KEY=
ENCRYPTION_KEY=
SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=
SPOTIFY_REDIRECT_URI=http://localhost:3000/auth/spotify/callback

# Optional for local sync-engine smoke command only (temporary token, never commit)
SPOTIFY_ACCESS_TOKEN=

# Deferred integrations / later phases
RAILWAY_AUTH_URL=
RESEND_API_KEY=
